import React from 'react'
import { Text, View, Button } from 'react-native'

export default function MembershipScreen({ navigation: { goBack } }) {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        </View>
    );
}
